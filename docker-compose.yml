services:
  app:
    build: .
    container_name: kelvinbward-app-1
    restart: unless-stopped
    networks:
      - web_gateway
    environment:
      - VIRTUAL_HOST=kelvinbward.localhost
      - VIRTUAL_PORT=80

networks:
  web_gateway:
    external: true
